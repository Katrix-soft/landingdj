services:
  landing-dj:
    build: .
    container_name: landing-dj
    restart: always
    expose:
      - 80
    environment:
      - VIRTUAL_HOST=dj.katrix.com.ar
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=dj.katrix.com.ar
      - LETSENCRYPT_EMAIL=exe20@gmail.com
    networks:
      - saavedra_net

networks:
  saavedra_net:
    driver: bridge
    name: saavedra_network # Esto crea una red "User-Defined" que sí soporta alias
